#include "main.h"

/**
 * _atoi - converts character to integer
 * @s: character to be converted
 *
 * Return: returns num if success
 *         otherwise returns 0
 */
int _atoi(char *s)
{
	char *str = s;
	int num;

	num = _atoi(str);
	return (num);
	return (0);
}
