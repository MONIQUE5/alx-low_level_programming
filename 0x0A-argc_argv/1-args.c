#include "main.h"
#include <stdlib.h>

/**
 * main - returns 0
 * @argc: counts number of arguments in a program
 * @argv: points to a string in a program
 *
 * Return: always returns 0
 */

int main(int argc, char __attribute__((__unused__)) *argv[])
{
printf("%d\n", argc - 1);
return (0);
}
