#include <stdio.h>

/**
 * main - entry point
 *
 *
 * printf: prints numbers less than 10
 *
 *
 * Return: always returns 0
 */

int main(void)
{
int num;
for (num = 0; num < 10; num++)
printf("%d", num);
printf("\n");
return (0);
}
